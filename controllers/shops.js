const Shop = require('../models/shop');

module.exports = {

};

// needs an index function to show a list of shops

// needs a create function to create a shop

// needs an update function to make changes to shop

// needs a delete function

// needs a detail function to display a shops info and items